# AIエージェント向け指示

## 目的

このリポジトリは、Shadowverse: Worlds Beyondの攻略知識管理アプリを扱う。
現在は要件をブラッシュアップしている段階である。ユーザーがブラッシュアップ済みの
要件とMVP範囲を明示的に承認するまで、本格的な実装を開始してはならない。

## ソース管理と作業管理

- ソースコードはこのリポジトリで管理し、設定後はGitHubを正本のリモートおよび
  コラボレーション基盤とする。
- 実装作業の正本はGitHub Issueとする。
- 1 Issueは、1つのユーザー価値または範囲が明確な技術作業に絞る。無関係な作業を
  同じIssueやプルリクエストへ混在させない。
- 原則として、1 Issueにつき1ブランチ・1プルリクエストとする。
- ブランチ名とプルリクエスト本文にはIssue番号を含める。
- Issueを完了するプルリクエストには `Fixes #<Issue番号>` を記載する。これにより、
  GitHub上でプルリクエストのマージ時にのみIssueを自動クローズできる。
- ユーザーの許可、またはその操作を明示的に許可する連携機能なしに、GitHub Issueや
  プルリクエストの作成・更新・クローズ・マージ・公開をしてはならない。

## Issueの品質

実装前に、Issueには次の内容を記載する。

1. 目的または解決したい問題
2. 必要に応じたユーザーストーリー
3. 検証可能な受け入れ条件
4. 明示的な対象外
5. 関連する技術的・設計上の制約

これらに重要な曖昧さがある場合、後戻りしにくいプロダクト判断をする前に、Issueを
整理するかユーザーへ確認する。

## 進捗管理

- GitHub Projectsを導入している場合、作業の進捗はProjectの `Status` を正本とする。
  推奨値は `Backlog`、`Ready`、`In Progress`、`In Review`、`Blocked`、`Done` とする。
- IssueのOpen / Closedは完了の最終事実とする。Issueを完了するプルリクエストには
  `Fixes #<Issue番号>` を記載し、受け入れ条件を満たした変更がマージされた場合に
  GitHubでIssueを自動クローズできる。
- `status:*` ラベルとProjectの `Status` を併用しない。Projects未導入時だけ、
  `status:ready`、`status:in-progress`、`status:review` を暫定的な進捗の正本として
  利用できる。Projects導入時には、進捗ラベルをProjectの `Status` へ移行して廃止する。
- Issueラベルは進捗ではなく、種別、優先度、担当領域など進捗と独立した分類に使う。

推奨ラベル：

- `type:feature`、`type:bug`、`type:research`、`type:chore`、`type:tech-debt`
- `priority:high`
- `area:domain`、`area:web`、`area:infra`

## AI支援の開発フロー

ユーザーから別の指示がない限り、次の順序で進める。

1. 要件や観察事項からIssueの下書きを作成する。
2. 必要な場合、外部のIssue起票前にユーザーの確認を得る。
3. 実装前に、Issueの受け入れ条件と対象外を確認する。
4. 範囲を守って実装し、必要なテストとドキュメントも更新する。
5. 該当するフォーマット、Lint、型検査、テスト、ビルドを実行する。
6. 変更概要、検証結果、制約を含むプルリクエスト本文を作成する。適切な場合は
   `Fixes #<Issue番号>` も含める。
7. 最終レビュー、マージ、デプロイなどの不可逆な外部操作は、ユーザーが明示的に
   委任しない限りユーザーに委ねる。

## 完了の定義

コードを書いただけで作業完了としてはならない。合意済みの範囲が実装され、
受け入れ条件が検証され、関連する自動チェックが成功し、必要なレビューまたはマージが
完了したときに完了とする。

## 自動化の境界

- AIは、Issue下書き、計画、実装変更、テスト、ドキュメント、プルリクエスト本文、
  低リスクな進捗更新を行える。
- AIは、作業範囲を黙って変更したり、自身でプロダクト判断を承認したり、根拠と権限
  なしにプルリクエストをマージ、デプロイ、または作業完了としてはならない。
- CI要件が合意された後は、定型的なチェックをGitHub Actionsで自動化する。最低限、
  フォーマットまたはLint、型検査、テスト、本番ビルドを実行する。
